info:
  name: save user
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/w/users/save
  body:
    type: json
    data: |-
      {
        "name": "Juan Pérez",
        "email": "juan.perez@email.com",
        "phone": "123456789",
        "profile": "ADMIN",
        "active": true,
        "emergencyMode": false
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
