info:
  name: create alert
  type: http
  seq: 10

http:
  method: POST
  url: http://localhost:8080/w/alerts
  body:
    type: json
    data: |-
      {
        "userId": 1,
        "description": "Alerta de prueba"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
